# Backend Environment Variables
# Copy this to .env (development) or set in Vercel dashboard (production)

# Server Configuration
PORT=8000
HOST=0.0.0.0
ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# LLM Integration (Optional - when adding Groq)
GROQ_API_KEY=your_groq_api_key_here

# Database Integration (Optional - when adding persistence)
DATABASE_URL=postgresql://user:password@localhost/agentflow

# Email Service (Optional - for real email sending)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Web Search (Optional - for real web searches)
SERPAPI_KEY=your_serpapi_key_here
